{
  "name": "servicenow-mcp-server",
  "version": "1.0.0",
  "description": "MCP server connecting to a real ServiceNow instance for Copilot Cowork plugin",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.1.0"
  }
}
